export interface IConfigTemplate {
    Name: string;
    DisplayName: string;
    PostScript: boolean;
    ScanScript: boolean;
    QueueScript: boolean;
    SchedulerScript: boolean;
    Template: string;
}
